#ifndef VALIDACOES_H
#define VALIDACOES_H

// --- Declarações das Funções de Validação ---
// Este ficheiro apenas "promete" que estas funções existem.
// A implementação delas está no ficheiro .c

int validar_apenas_letras(const char* str);
int validar_cpf(const char* cpf);
int validar_cnpj(const char* cnpj);
int validar_telefone(const char* telefone);
int validar_email(const char* email);

#endif // Fim de VALIDACOES_H

